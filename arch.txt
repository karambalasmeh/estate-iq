🗺️ الهيكل العام للمشروع (The Anatomy)
المشروع مقسم لـ 4 أجزاء رئيسية:

الجذر (Configuration & Env): الإعدادات العامة.

الدماغ (ML): الموديل والتدريب.

الخلفية (Backend): السيرفر والمنطق وقواعد البيانات.

الواجهة (Frontend): ما يراه المستخدم.

1. الجذر (Root Directory) - "غرفة القيادة"
هنا الملفات التي تدير المشروع ككل.

.env:

الدور: "الخزنة السرية".

الوظيفة: يحفظ الأسرار (رابط الداتا بيس، كلمة سر التشفير). الكود يقرأ منه ولا نكتب الأسرار في الكود مباشرة.

docker-compose.yml:

الدور: "قائد الأوركسترا".

الوظيفة: ملف يقول للدوكر: "أرجوك شغل لي داتا بيس Postgres نسخة 15 وافتح لها البورت 5433". حالياً يشغل الداتا بيس فقط، ولاحقاً سيشغل التطبيق كله.

requirements.txt:

الدور: "قائمة المشتريات".

الوظيفة: فيه أسماء كل المكتبات (FastAPI, SQLAlchemy, Scikit-learn) التي يحتاجها المشروع ليعمل.

.gitignore:

الدور: "الحارس الشخصي".

الوظيفة: يقول لـ Git: "تجاهل هذه الملفات (مثل venv و .env) ولا ترفعها على النت".

2. الدماغ (Machine Learning) - "المصنع"
scripts/train_model.py:

الدور: "المعلم" (The Trainer).

الوظيفة: كود منفصل عن السيرفر. وظيفته قراءة البيانات، تدريب الذكاء الاصطناعي، ثم حفظ النتيجة في ملف .pkl.

models/house_price_model.pkl:

الدور: "الدماغ المحفوظ" (Artifact).

الوظيفة: ملف ثنائي (Binary) يحتوي على معادلات الرياضيات التي تعلمها الموديل. السيرفر يقوم بتحميله ليتوقع الأسعار.

3. الخلفية (Backend - app/) - "المحرك"
هذا هو الجزء الأكبر والأعقد، ويتبع الـ Clean Architecture:

🅰️ app/core/ (النواة)
config.py:

الوظيفة: يقرأ ملف .env ويحوله لمتغيرات بايثون (settings.DATABASE_URL). هو "لوحة العدادات".

security.py:

الوظيفة: "ضابط الأمن". يحتوي دوال تشفير الباسوردات (Hashing) ودوال صناعة التوكن (JWT).

🅱️ app/db/ & app/db_models/ (الذاكرة)
db/session.py:

الوظيفة: "خط الهاتف". هو المسؤول عن فتح وإغلاق الاتصال مع قاعدة البيانات (PostgreSQL).

db_models/user.py:

الوظيفة: يصف شكل جدول المستخدمين (users) للداتا بيس (الأعمدة: id, email, password).

db_models/prediction.py:

الوظيفة: يصف شكل جدول التاريخ (predictions) والعلاقة مع المستخدم.

🆎 app/schemas/ (العقود - Pydantic)
الوظيفة: "الجمارك". تتأكد أن البيانات الداخلة والخارجة سليمة.

house.py: يفحص مدخلات البيت (هل الغرف رقم؟ هل المساحة موجبة؟).

user.py: يفحص مدخلات التسجيل (هل الإيميل صحيح؟).

token.py: يحدد شكل التوكن الذي يرجع للمستخدم.

🧠 app/services/ (المنطق)
model_service.py:

الوظيفة: يحمل ملف .pkl من القرص إلى الرام مرة واحدة (Singleton)، ويحتوي دالة predict التي تأخذ الأرقام وتعطي السعر.

auth_service.py:

الوظيفة: يأخذ بيانات التسجيل، ينادي security.py لتشفير الباسورد، ثم يحفظ المستخدم في الداتا بيس.

🚪 app/api/ (البوابات)
deps.py:

الوظيفة: "الحارس اللي عالباب". يأخذ التوكن من الهيدر، يتأكد أنه صالح، ويرجع هوية المستخدم. نستخدمه لحماية الروابط.

v1/endpoints/auth.py:

الوظيفة: شباك التذاكر (Signup/Login). يستقبل الإيميل والباسورد ويعطي التوكن.

v1/endpoints/prediction.py:

الوظيفة: شباك الخدمة. يستقبل مواصفات البيت (محمية بالتوكن)، ينادي الـ Service للتوقع، يحفظ النتيجة في الـ DB، ويرجع السعر.

🏁 app/main.py (المدير العام)
الوظيفة: هو نقطة البداية.

يجمع كل الـ Routers (auth, prediction).

يُنشئ الجداول في الداتا بيس إذا لم تكن موجودة.

يُعرف مكان ملفات الـ HTML (Static Files).

يشغل التطبيق.

4. الواجهة (Frontend - app/static/) - "الديكور"
index.html: الصفحة الرئيسية (Landing Page).

login.html & signup.html: صفحات الدخول والتسجيل.

dashboard.html: صفحة تعرض تاريخ التوقعات.

style.css: ملف التجميل والألوان (التصميم الخرافي).

script.js:

الدور: "المراسل".

الوظيفة: كود جافاسكريبت يعمل في متصفح المستخدم. يأخذ البيانات من الحقول، يرسلها للـ API، ينتظر الرد، ويعرض النتيجة على الشاشة. هو من يتعامل مع localStorage لحفظ التوكن.

🔄 كيف تتواصل هذه الملفات؟ (سيناريو: مستخدم يعمل توقع)
Frontend (script.js): يجمع مواصفات البيت + يأخذ التوكن من المتصفح -> يرسل طلب POST /predict للسيرفر.

Manager (main.py): يستلم الطلب ويوجهه لـ prediction.py.

Guard (deps.py): يوقف الطلب: "أين التوكن؟". يفحصه بـ security.py ويتأكد أن المستخدم موجود في db. إذا تمام، يسمح بالمرور.

Router (prediction.py):

ينادي model_service.py: "يا موديل، كم سعر هذا البيت؟".

الموديل يحسب ويرجع الرقم.

الراوتر ينادي db/session.py: "احفظ هذه العملية في جدول predictions لهذا المستخدم".

Database (Postgres): تخزن السطر الجديد.

Response: السيرفر يرجع السعر للـ script.js.

User: يرى السعر على الشاشة "💎 Estimated Price...".